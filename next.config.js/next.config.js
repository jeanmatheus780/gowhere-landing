/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Ativa o modo estrito do React
    experimental: {
      appDir: true, // Ativa a estrutura do app (novos recursos do Next.js 13)
    },
  }
  
  module.exports = nextConfig;
  